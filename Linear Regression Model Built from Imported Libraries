#import all other libraries
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split
from sklearn.metrics import mean_squared_error, r2_score

from google.colab import files
uploaded = files.upload()

import pandas as pd
import io
df = pd.read_excel(io.BytesIO(uploaded['Work_data (4).xlsx']), header=1)
df = df.drop(columns=['Unnamed: 0'])
df

X = df["PH"].values
X

Y = df["EC"].values
Y

X=X.reshape(-1,1)
X

y_pred = lr.predict(x_test)
r2 = r2_score(y_test, y_pred)
print(f"R^2: {r2}")

import matplotlib.pyplot as plt
plt.scatter(x_train,y_train, color="black", label= "dot")
plt.plot(x_test,y_pred, color ="red", label="RegressionLine")
plt.grid(True, alpha =0.1)
plt.legend()

#Predicted values
predicted_values = pd.DataFrame({'Actual': y_test, 'Predicted': y_pred, 'Difference':y_test-y_pred})
print(predicted_values)
